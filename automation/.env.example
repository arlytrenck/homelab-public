# automation — env vars referenced by docker-compose.yaml.
# Generated by tools/gen-env-examples.sh — do not hand-edit; re-run it.
# Real values go in automation/.env (gitignored). Names + compose defaults only here.

N8N_ENCRYPTION_KEY=
