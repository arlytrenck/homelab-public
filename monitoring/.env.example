# monitoring — env vars referenced by docker-compose.yaml.
# Generated by tools/gen-env-examples.sh — do not hand-edit; re-run it.
# Real values go in monitoring/.env (gitignored). Names + compose defaults only here.

GOTIFY_DEFAULTUSER_PASS=changeme-long-random
GOTIFY_TOKEN_ALERTMANAGER=
