# immich — env vars referenced by docker-compose.yml.
# Generated by tools/gen-env-examples.sh — do not hand-edit; re-run it.
# Real values go in media/immich/.env (gitignored). Names + compose defaults only here.

DB_DATABASE_NAME=
DB_DATA_LOCATION=./postgres
DB_PASSWORD=
DB_USERNAME=
IMMICH_VERSION=release
UPLOAD_LOCATION=./library
