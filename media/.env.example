# media — env vars referenced by docker-compose.yaml.
# Generated by tools/gen-env-examples.sh — do not hand-edit; re-run it.
# Real values go in media/.env (gitignored). Names + compose defaults only here.

JELLYSTAT_DB_PASSWORD=
JELLYSTAT_JWT_SECRET=
TMM_VNC_PASSWORD=
