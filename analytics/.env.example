# analytics — env vars referenced by docker-compose.yaml.
# Generated by tools/gen-env-examples.sh — do not hand-edit; re-run it.
# Real values go in analytics/.env (gitignored). Names + compose defaults only here.

UMAMI_APP_SECRET=changeme-long-random
UMAMI_DB_PASS=changeme-long-random
